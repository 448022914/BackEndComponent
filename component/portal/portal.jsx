import React from "react";
import styles from "./portal.scss";

export default class Portal extends React.Component{
  constructor(props){
    super(props);
  }
  render(){
    return(
      <div className={styles["portal-box"]}>
        
      </div>
    );
  }
}